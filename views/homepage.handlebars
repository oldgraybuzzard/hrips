{{#unless loggedIn}}
<h1>You must login first!</h1>
{{/unless}}

{{#if loggedIn}}
<section>
  <h2>Create New Employee</h2>

  <form class="new-employee-form">
    <div>
      <label for="first_name">Employee First Name</label>
      <input type="text" id="first_name" name="first_name" />
    </div>
    <div>
      <label for="last_name">Last Name</label>
      <input type="text" id="last_name" name="last_name" />
    </div>
    <div>
      <label for="email">Email</label>
      <input type="email" id="email" name="email" />
    </div>
    <div>
      <label for="manager_id">Manager ID</label>
      <input type="manager_id" id="manager_id" name="manager_id" />
    </div>
    <div>
      <label for="role_id">Job Title</label>
      <input type="role_id" id="role_id" name="role_id" />
    </div>        
    <button type="submit" class="btn btn-primary">Create</button>
  </form>
</section>

<hr>

{{!-- {{#if employees.length}}
<section>
  
  <ol>
    {{#each employees as |employee|}}
    <li>
      {{> employee-info employee}}
      <a href="/dashboard/edit/{{employee.id}}" class="edit-link">Edit employee</a>
    </li>
    {{/each}}
  </ol>
</section>
{{/if}} --}}

{{#if employees.length}}
<section>
  <h2>List of Company Employees</h2>  
  {{#each employees as |employees|}}
  <ol class="employee-list">
          {{> employee-info employees}}
  </ol>
  {{/each}}
</section>
{{/if}}
{{/if}}


<script src="/javascript/add-employee.js"></script>
